
$(document).ready(function(){
/**

var trigger = setInterval(function(){
		var hash = window.location.hash;

		if(hash == "#MI5"){
			if($('.btn-primary:eq(0)').length){
				$('.btn-primary')[0].click();
				clearInterval(trigger);
			}
			$('p.desc').html("Script is trying to buy Mi5. <br/> This is not auto buy. It will auto complete most of the steps.<br/> You might have need to click few buttons.");
		}else if(hash == "#HM3A"){
			if($('.btn-primary:eq(1)').length){
				$('.btn-primary')[1].click();
				clearInterval(trigger);
			}
			$('p.desc').html("Script is trying to buy Redmi Note 3. <br/> This is not auto buy. It will auto complete most of the steps.<br/> You might have need to click few buttons.");
		}else if(hash == "#MIPOWER20000"){
			if($('.btn-primary:eq(2)').length){
				$('.btn-primary')[2].click();
				clearInterval(trigger);
			}
			$('p.desc').html("Script is trying to buy Mi Power Bank. <br/> This is not auto buy. It will auto complete most of the steps.<br/> You might have need to click few buttons.");
		}
},10);


**/

});
